// import 'mocha'
// import fc from 'fast-check'
// import { isMatcher } from '../src/isMatcher'
// import { aMatcher } from './generators'

// describe('isMatcher', () => {
//   it('should return', () => {
//     fc.assert(fc.property(aMatcher, (a) => {isMatcher(a)}), {numRuns: 1000})
//   }).timeout(100_000)
// })
