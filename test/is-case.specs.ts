// import 'mocha'
// import fc from 'fast-check'
// import { isCase } from '../src/isMatcher'
// import { tupleValidatorAndHandler } from './generators'

// describe('isCase', () => {
//   it('should return', () => {
//     fc.assert(fc.property(tupleValidatorAndHandler, (a) => {isCase(a)}), {numRuns: 1000})
//   }).timeout(100_000)
// })
